# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
server/*.db
server/*.db-wal
server/*.db-shm
pterodactyl/data

# Real credentials that live in the working tree, not just .env:
# WireGuard keypair for the relay tunnel, and the live Wings daemon's own
# auth token (embedded inline in its config, not in a separate env file).
node1_private.key
node1_public.key
pterodactyl/wings/etc/config.yml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
